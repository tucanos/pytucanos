[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "pytucanos"
dependencies = [
  "numpy",
  "matplotlib"
]

[tool.maturin]
profile = "release"
# features = ["metis", "scotch"]